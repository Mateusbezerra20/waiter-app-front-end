import styled from "styled-components";

export const Wrapper = styled.div`
  span {
    font-size: 0.8rem;
    line-height: 200%;
    font-weight: 500;
  }

  .input-currency {
    border: 1px solid #ccc;
    border-radius: 8px;
    padding: 1rem 0.8rem;
    font-size: 0.8rem;
    font-weight: 500;
    outline: none;
  }
`;
